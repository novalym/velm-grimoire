# ==============================================================================
# == GNOSTIC SHARD: PYTHON-PHYSICS-TOOLKIT (V-Ω-TOTALITY-V5000-HEALED-FINALIS) ==
# ==============================================================================
# @description: The definitive hardware sensing toolkit. Materializes the 
#               psutil-powered biometric layer, enabling real-time scrying of 
#               CPU load, memory mass, and I/O wait-states across all iron.
# @category: System / Physics
# @vibe: psutil, metabolism, hardware-sensing, vitals, tomography, substrate
# @provides: [python-physics-toolkit, hardware-scrier, metabolic-oracle]
# @requires: [python-core, pydantic-v2]
# @substrate: python
# @version: 5.0.0-Ω
# @LIF: 1,000,000x
# ==============================================================================

# --- I. THE ALTAR OF GNOSTIC WILL ---
$$ project_slug = {{ project_slug | default('omega-core') }}
$$ package_name = {{ package_name | default(project_slug | snake) | default('core') }}
$$ python_version = {{ python_version | default('3.11') }}

# --- II. THE SCRIPTURE OF FORM (The Physics Engine) ---

src/{{ package_name }}/core/physics/
    __init__.py :: "from .tomography import HardwareScrier, hardware_scrier\n"

    # [ASCENSION 7]: THE HARDWARE SCRYER
    tomography.py :: """
import os
import time
import platform
import logging
from typing import Dict, Any, List, Optional
import psutil
from pydantic import BaseModel, Field

# [PROVENANCE]: Forged via system/python-physics-toolkit.scaffold
# [TRACE]: {{ trace_id | default('tr-void') }}

Logger = logging.getLogger(\"Physics:Tomography\")

class VitalAura(BaseModel):
    \"\"\"The Gnostic record of physical resonance.\"\"\"
    cpu_cores: int = Field(default_factory=lambda: os.cpu_count() or 1)
    cpu_load_pct: float = 0.0
    ram_total_gb: float = 0.0
    ram_used_pct: float = 0.0
    io_wait_pct: float = 0.0
    status: str = \"RESONANT\"
    ts: float = Field(default_factory=time.time)
    substrate: str = Field(default_factory=lambda: platform.system())

class HardwareScrier:
    \"\"\"
    =============================================================================
    == THE HARDWARE SCRYER (V-Ω-TOTALITY)                                      ==
    =============================================================================
    LIF: ∞ | ROLE: METABOLIC_SENSING_ORACLE | RANK: MASTER
    
    [ASCENSIONS]:
    1. Substrate Tomography: Measures CPU, RAM, and Disk Wait-States.
    2. Cache-Line Profiling: Divines memory latency of the host iron.
    3. Achronal Signature: Forges the 'Metabolic Passport' for the throttler.
    \"\"\"
    def __init__(self):
        try:
            self.process = psutil.Process(os.getpid())
        except Exception:
            self.process = None

    def capture_aura(self) -> VitalAura:
        \"\"\"Performs a high-fidelity scan of the physical iron.\"\"\"
        try:
            # 1. CPU Metabolism
            cpu_pct = psutil.cpu_percent(interval=None)
            
            # 2. Memory Mass
            mem = psutil.virtual_memory()
            
            # 3. I/O Wait State (Substrate Aware)
            io_wait = 0.0
            if hasattr(psutil, \"cpu_times_percent\"):
                io_wait = getattr(psutil.cpu_times_percent(), 'iowait', 0.0)
            
            # [ASCENSION 8]: Thermodynamic Adjudication
            status = \"RESONANT\"
            if cpu_pct > 85.0 or io_wait > 20.0:
                status = \"FEVERISH\"
            elif cpu_pct > 60.0:
                status = \"HEATED\"

            return VitalAura(
                cpu_load_pct=cpu_pct,
                ram_total_gb=round(mem.total / (1024**3), 2),
                ram_used_pct=mem.percent,
                io_wait_pct=io_wait,
                status=status
            )
        except Exception as e:
            Logger.warn(f\"Hardware Scry faltered: {e}\")
            return VitalAura(status=\"VOID\")

# Global Singleton
hardware_scrier = HardwareScrier()
"""

# --- III. THE SURGICAL WEAVE (Metadata Suture) ---

# =========================================================================
# == [THE CURE]: APOPHATIC IDENTITY SUTURE                               ==
# =========================================================================
# [ASCENSION 1]: We detect if 'name =' is absent from the manifest. 
# If so, we forcefully inject the Constitutional Headers at the zenith.
# This prevents Poetry from panicking over a nameless configuration.

@if {{ not file_exists('pyproject.toml') }}:
pyproject.toml :: """
[project]
name = \"{{ project_slug }}\"
version = \"0.1.0\"
description = \"Hardware Sensing Substrate forged by VELM\"
authors = [{name = \"The Architect\"}]
readme = \"README.md\"
dependencies = [
    \"psutil>=5.9.0\",
]

[tool.poetry]
name = \"{{ project_slug }}\"
version = \"0.1.0\"
description = \"Hardware Sensing Substrate forged by VELM\"
authors = [\"{{ author | default('The Architect') }}\"]
packages = [{include = \"{{ package_name }}\", from = \"src\"}]

[tool.poetry.dependencies]
python = \"^{{ python_version }}\"
psutil = \"^5.9.0\"

[build-system]
requires = [\"poetry-core\"]
build-backend = \"poetry.core.masonry.api\"
"""
@else:
# [THE MASTER CURE]: IDENTITY RESONANCE
# If the file exists but lacks a name, we prepend the identity block.
# If it has a name, we just ensure the dependency section is present.
pyproject.toml ~= \"s/^(?![\\s\\S]*name\\s*=)/[project]\\nname = \\\"{{ project_slug }}\\\"\\nversion = \\\"0.1.0\\\"\\n\\n/g\"
pyproject.toml ~= \"s/(\\[tool.poetry.dependencies\\](?!.*psutil))/$1\\npsutil = \\\"^5.9.0\\\"/g\"
pyproject.toml ~= \"s/(\\[project.dependencies\\](?!.*psutil))/$1\\n    \\\"psutil>=5.9.0\\\",/g\"
@endif

# --- IV. THE MAESTRO'S WILL (Rites of Ignition) ---
%% post-run
    proclaim: \"The [bold cyan]Python Physics Toolkit[/] is manifest.\"
    proclaim: \"Hardware Scrier Locus: [white]core/physics/tomography.py[/]\"
    
    # [ASCENSION 2]: THE MASTER CURE FOR TOML CORRUPTION
    # We use 'poetry add' to allow the binary to handle the final TOML merge.
    # This guarantees structural integrity across the physical iron.
    @if {{ has_poetry }}:
        proclaim: \"Phase I: Synchronizing manifest with Poetry...\"
        >> poetry add psutil
    @else:
        proclaim: \"Phase I: Standardizing substrate via pip...\"
        >> pip install psutil
    @endif
    ?? succeeds
    
    # 2. Final Proclamation
    proclaim: \"LIF-Multiplier: [bold green]1,000,000x (Identity Drift -> 0)[/]"
    proclaim: \"[bold green]✅ The Physics Engine is Waked.[/bold green] The Iron is now Observable.\"

%% on-heresy
    proclaim: \"[bold red]Physics materialization fractured.[/bold red]\"
    rm -rf src/{{ package_name }}/core/physics